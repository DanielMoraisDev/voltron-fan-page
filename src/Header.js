import { styled } from "styled-components";

export const MainNav = styled.main`
    display: flex;
    flex-direction: row;
    justify-content: space-around;
    align-items: center;
    height: 4vh;
    gap: 10px;
`